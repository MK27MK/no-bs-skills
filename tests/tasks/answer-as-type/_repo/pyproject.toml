[project]
name = "shop"
version = "0.4.2"
requires-python = ">=3.11"
dependencies = ["sqlalchemy>=2.0", "httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
