{
  "arm": "with",
  "prompt": "/fill-the-gaps Write the bodies in stats.py. Empty input should raise ValueError.",
  "model": "claude-sonnet-5",
  "message": "Filled both bodies using only builtins (`sum`, `len`, `sorted`) \u2014 no new definitions needed, so `lock_definitions` stays satisfied.\n\nBoth function names are misspelled (`calcualte_avrage` \u2192 `calculate_average`, `calcualte_median` \u2192 `calculate_median`), but `lock_names` defaults to `y`, so I kept them as-is per the lock rules. Say the word if you want me to apply that rename and fix callers.",
  "diff": "diff --git a/stats.py b/stats.py\nindex 29085f5..0db4b8c 100644\n--- a/stats.py\n+++ b/stats.py\n@@ -1,6 +1,14 @@\n def calcualte_avrage(values: list[float]) -> float:\n-    ...\n+    if not values:\n+        raise ValueError(\"values must not be empty\")\n+    return sum(values) / len(values)\n \n \n def calcualte_median(values: list[float]) -> float:\n-    ...\n+    if not values:\n+        raise ValueError(\"values must not be empty\")\n+    ordered = sorted(values)\n+    mid = len(ordered) // 2\n+    if len(ordered) % 2:\n+        return ordered[mid]\n+    return (ordered[mid - 1] + ordered[mid]) / 2\n",
  "wip_diff": "diff --git a/stats.py b/stats.py\nnew file mode 100644\nindex 0000000..29085f5\n--- /dev/null\n+++ b/stats.py\n@@ -0,0 +1,6 @@\n+def calcualte_avrage(values: list[float]) -> float:\n+    ...\n+\n+\n+def calcualte_median(values: list[float]) -> float:\n+    ...\n",
  "cost_usd": 0.0690258,
  "num_turns": 3,
  "is_error": false
}
